{
  format_version = 2  # Required for all NVFLARE v2.x jobs.

  executors = [
    {
      tasks = ["train"]
      executor = {
        path = "nvflare.app_opt.pt.pt_trainer.PTTrainer"
      }
    }
  ]

  task_data_filters = []
  task_result_filters = []
  components = []
}